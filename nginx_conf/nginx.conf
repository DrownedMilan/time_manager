events {}

http {
    server {
        listen 80;

        # Frontend (React build)
        location / {
            root /usr/share/nginx/html;
            try_files $uri /index.html;
        }

        # Backend API proxy
        location /api/ {
            proxy_pass http://backend:8000;
        }
    }
}
